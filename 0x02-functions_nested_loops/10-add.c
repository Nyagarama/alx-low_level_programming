#include "main.h"
#include <stdio.h>

/**
 * add - addition of two integers
 * @a: first integer value
 * @b: second integer value
 * Return: total.
 */
int add(int a, int b)
{
	int total;

	total = a + b;
	return (total);
}
